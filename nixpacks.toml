[phases.setup]
nixPkgs = ["nodejs_20", "ffmpeg", "python3", "gnumake", "gcc"]

[phases.install]
cmds = [
  "npm install --legacy-peer-deps --ignore-scripts",
  "npm rebuild sharp --ignore-scripts=false || true"
]

[start]
cmd = "bash start.sh"
